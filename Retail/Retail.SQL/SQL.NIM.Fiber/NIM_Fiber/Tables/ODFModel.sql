CREATE TABLE [NIM_Fiber].[ODFModel] (
    [ID]               INT        NOT NULL,
    [LastModifiedTime] DATETIME   NULL,
    [timestamp]        ROWVERSION NULL,
    PRIMARY KEY CLUSTERED ([ID] ASC)
);

