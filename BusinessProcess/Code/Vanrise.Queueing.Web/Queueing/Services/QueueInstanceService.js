(function (appControllers) {

    'use strict';

    QueueInstanceService.$inject = ['VRModalService'];

    function QueueInstanceService(VRModalService) {
        return ({
            
        });

    

    };

    appControllers.service('VR_Queueing_QueueInstanceService', QueueInstanceService);

})(appControllers);
